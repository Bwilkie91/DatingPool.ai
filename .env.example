# Optional: override waitlist form URL (used when user joins waitlist).
# If unset, falls back to placeholder; set in production to your real form (e.g. Google Form).
# VITE_WAITLIST_URL=https://forms.gle/your-waitlist-link
